import { COLORS } from './colors';

export const REVIEW_HISTORY = {
  maxRating: 5,
  ratingIconSize: 16,
  ratingRowGap: 4,
  cardBackground: COLORS.surface,
  cardBorderRadius: 12,
  cardBorderWidth: 1,
} as const;
